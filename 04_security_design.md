# セキュリティ設計

- [1. セキュリティ設計方針](#1-セキュリティ設計方針)
- [2. 認証方式](#2-認証方式)
  - [2.1 店舗スタッフ・店長・本部管理者向け認証](#21-店舗スタッフ店長本部管理者向け認証)
  - [2.2 来店客向けAPIの扱い](#22-来店客向けapiの扱い)
- [3. 認可方式](#3-認可方式)
  - [3.1 ロールベースアクセス制御](#31-ロールベースアクセス制御)
  - [3.2 ロール階層](#32-ロール階層)
  - [3.3 店舗単位アクセス制御](#33-店舗単位アクセス制御)
- [4. JWT設計](#4-jwt設計)
  - [4.1 JWTの発行元](#41-jwtの発行元)
  - [4.2 Spring Boot APIで検証する内容](#42-spring-boot-apiで検証する内容)
  - [4.3 JWTから取得するユーザー情報](#43-jwtから取得するユーザー情報)
- [5. Cognito設計](#5-cognito設計)
  - [5.1 User Pool](#51-user-pool)
  - [5.2 App Client](#52-app-client)
  - [5.3 Group](#53-group)
  - [5.4 User](#54-user)
- [6. 来店客向けQR注文セキュリティ](#6-来店客向けqr注文セキュリティ)
  - [6.1 固定QRトークンの扱い](#61-固定qrトークンの扱い)
  - [6.2 来店セッションの扱い](#62-来店セッションの扱い)
  - [6.3 注文可能条件](#63-注文可能条件)
  - [6.4 不正アクセス対策](#64-不正アクセス対策)
  - [6.5 異常注文対策](#65-異常注文対策)
- [7. API別認証・認可方針](#7-api別認証認可方針)
- [8. パスワード管理方針](#8-パスワード管理方針)
- [9. セッション管理方針](#9-セッション管理方針)
- [10. CORS方針](#10-cors方針)
- [11. CSRF方針](#11-csrf方針)
- [12. ログ出力方針](#12-ログ出力方針)
  - [12.1 ログ出力対象](#121-ログ出力対象)
  - [12.2 ログに含める情報](#122-ログに含める情報)
  - [12.3 ログレベル方針](#123-ログレベル方針)
  - [12.4 機密情報のログ出力禁止](#124-機密情報のログ出力禁止)
  - [12.5 CloudWatch連携方針](#125-cloudwatch連携方針)
- [13. セキュリティテスト観点](#13-セキュリティテスト観点)
  - [13.1 認証テスト](#131-認証テスト)
  - [13.2 ロール認可テスト](#132-ロール認可テスト)
  - [13.3 店舗単位アクセス制御テスト](#133-店舗単位アクセス制御テスト)
  - [13.4 来店客向けQR注文制御テスト](#134-来店客向けqr注文制御テスト)
  - [13.5 異常注文テスト](#135-異常注文テスト)
  - [13.6 CSRF・CORSテスト](#136-csrfcorsテスト)
  - [13.7 ログ出力テスト](#137-ログ出力テスト)

## 1. セキュリティ設計方針

本システムでは、店舗スタッフ・店長・本部管理者が利用する業務APIと、来店客が利用するQR注文APIを分けてセキュリティを設計する。

クライアントは、来店客が利用する注文画面と、店舗スタッフ・店長・本部管理者が利用する業務管理画面の2種類とする。
業務管理画面は1つの共通フロントとして扱い、ログインユーザーのロールによって表示メニューと操作可能機能を切り替える。
ただし、画面上で非表示にするだけではなく、API側でも必ずロール認可と店舗単位アクセス制御を行う。

店舗スタッフ、店長、本部管理者向けAPIでは、Amazon Cognito による OAuth2.0 / OpenID Connect 認証を利用する。
Cognito User Pool が JWT アクセストークンを発行し、Spring Boot API は OAuth2 Resource Server として JWT を検証する。

来店客向けAPIは Cognito 認証の対象外とする。
来店客はテーブル上の固定QRコードから注文画面へアクセスするが、QRコード自体を注文権限としては扱わない。
注文可否は、QRトークン、来店セッショントークン、テーブルステータス、来店セッション状態、有効期限によりサーバー側で判定する。

本システムのセキュリティ設計では、以下を基本方針とする。

| No  | 方針             | 内容                                                                  |
| --- | ---------------- | --------------------------------------------------------------------- |
| 1   | 認証方式の分離   | スタッフ系APIはJWT認証、来店客向けAPIはQR注文制御で扱う               |
| 2   | パスワード非保持 | アプリケーションDBの users テーブルにはパスワードを保持しない         |
| 3   | ロール認可       | ADMIN、STORE_MANAGER、STORE_STAFF の業務ロールで利用可能APIを制御する |
| 4   | 店舗単位認可     | 店舗スタッフ・店長は自店舗以外のデータを参照・更新できない            |
| 5   | サーバー側判定   | 画面表示やURLだけに依存せず、API側で認証・認可・注文可否を判定する    |
| 6   | 監査性           | スタッフ操作は、対象テーブルに監査項目が存在する場合、users.id を基に created_by / updated_by へ記録する |

## 2. 認証方式

### 2.1 店舗スタッフ・店長・本部管理者向け認証

店舗スタッフ、店長、本部管理者向けAPIでは、Amazon Cognito による OAuth2.0 / OpenID Connect 認証を利用する。
Cognito User Pool が JWT アクセストークンを発行し、Spring Boot API は OAuth2 Resource Server として JWT を検証する。

Spring Boot API ではフォームログイン、Basic認証、アプリケーション独自のパスワード認証は行わない。
ログイン画面、パスワード入力、パスワード検証、パスワードポリシーは Cognito 側で管理する。

業務APIへアクセスするクライアントは、Cognito から取得したアクセストークンを HTTP リクエストの Authorization ヘッダーに設定する。

```http
Authorization: Bearer {access_token}
```

認証が必要なAPIで Authorization ヘッダーが存在しない、Bearer形式ではない、JWTが無効、期限切れ、または検証に失敗した場合は `401 Unauthorized` を返す。

対象となるAPI区分は以下とする。

| API区分       | 認証方式        | 説明                                       |
| ------------- | --------------- | ------------------------------------------ |
| 店舗業務API   | Cognito JWT認証 | 店舗スタッフ・店長が利用する               |
| 本部管理API   | Cognito JWT認証 | 本部管理者が利用する                       |
| バッチ管理API | Cognito JWT認証 | 本部管理者、または運用管理者が利用する想定 |

### 2.2 来店客向けAPIの扱い

来店客向けAPIは Cognito 認証の対象外とする。
来店客は users テーブルでは管理せず、Cognito ユーザーとしても登録しない。

来店客はテーブル上の固定QRコードを読み取り、QRトークンを含むURLから注文画面へアクセスする。
ただし、固定QRトークンはテーブルを識別するための情報であり、固定QRトークン単体では注文権限として扱わない。

注文可否は、サーバー側で以下を組み合わせて判定する。

| No  | 判定項目               | 内容                                                   |
| --- | ---------------------- | ------------------------------------------------------ |
| 1   | QRトークン             | QRトークンに紐づくテーブルが存在すること               |
| 2   | 来店セッショントークン | 対象テーブルの有効な visitToken であること             |
| 3   | テーブルステータス     | テーブルが注文可能な状態であること                     |
| 4   | 来店セッション状態     | visit_sessions.status が ACTIVE であること             |
| 5   | 有効期限               | visit_sessions.expires_at が現在時刻より後であること   |
| 6   | メニュー状態           | 注文対象メニューが対象店舗に所属し、販売可能であること |

visit_sessions.expires_at は通常の注文可否を制御する主条件ではなく、放置された来店セッションや不正利用を防ぐための最大有効期限として扱う。
QRコードを第三者が読み取った場合でも、来店セッションが開始されていない、期限切れ、会計済み、またはテーブルが注文可能状態でない場合は注文を許可しない。

## 3. 認可方式

本システムでは、ロールベースアクセス制御と店舗単位アクセス制御を組み合わせる。

ロールベースアクセス制御では、ADMIN、STORE_MANAGER、STORE_STAFF の業務ロールに応じて利用可能APIを制御する。
店舗単位アクセス制御では、STORE_MANAGER および STORE_STAFF が自店舗以外のデータを参照・更新できないようにする。

Spring Boot API は、JWT の subject から CognitoユーザーIDを取得し、users テーブルを参照して業務ロールと所属店舗IDを取得する。
API認可では、JWTの有効性、業務ロール、所属店舗IDを組み合わせて判定する。

### 3.1 ロールベースアクセス制御

ロールベースアクセス制御では、APIごとに必要な業務ロールを定義し、ログインユーザーが必要ロールを満たす場合のみアクセスを許可する。

業務ロールはアプリケーションDBの users.role を正とする。
JWT内のロール情報は補助的に参照できるが、最終的な業務認可は users テーブルから取得した role を基に判定する。
Cognito Group と users.role が一致しない場合でも、業務認可では users.role を正とする。
Cognito Group は認証基盤側の補助情報として扱い、アプリケーション上の最終的な権限制御は users.role により判定する。

| ロール        | 主な利用者   | 利用可能範囲                                                 |
| ------------- | ------------ | ------------------------------------------------------------ |
| ADMIN         | 本部管理者   | 全店舗情報、全店舗売上、スタッフ管理、バッチ管理             |
| STORE_MANAGER | 店長         | 自店舗売上、自店舗メニュー管理、自店舗注文参照               |
| STORE_STAFF   | 店舗スタッフ | 自店舗のテーブル管理、注文確認、調理ステータス更新、会計処理 |

必要ロールを満たさない場合は `403 Forbidden` を返す。

### 3.2 ロール階層

本システムでは、上位ロールが下位ロールの権限を内包する。

| 上位ロール    | 内包するロール             |
| ------------- | -------------------------- |
| ADMIN         | STORE_MANAGER、STORE_STAFF |
| STORE_MANAGER | STORE_STAFF                |
| STORE_STAFF   | なし                       |

必要ロール欄に `STORE_STAFF` と記載されているAPIは、ロール階層により `STORE_MANAGER` および `ADMIN` も利用可能とする。
必要ロール欄に `STORE_MANAGER` と記載されているAPIは、ロール階層により `ADMIN` も利用可能とする。

ただし、ロール階層によってAPI利用権限を満たす場合でも、店舗単位アクセス制御の対象となるAPIでは所属店舗IDの判定を行う。

### 3.3 店舗単位アクセス制御

店舗単位アクセス制御では、STORE_MANAGER および STORE_STAFF が自店舗以外のデータを参照・更新できないようにする。

店舗業務APIでは、URL上の `storeId` と、JWT の subject から特定した users.store_id を比較する。
不一致の場合は `403 Forbidden` を返す。

```text
URLの storeId == users.store_id の場合のみアクセス許可
```

ADMIN は全店舗データにアクセスできるため、原則として店舗単位アクセス制御の制限を受けない。
ただし、ADMIN が店舗業務APIを代行操作する場合は、監査ログ上で操作ユーザーを識別できるようにする。

店舗単位アクセス制御の基本方針は以下とする。

| 対象ロール    | storeId 判定 | 内容                                            |
| ------------- | ------------ | ----------------------------------------------- |
| ADMIN         | 不要         | 全店舗データにアクセス可能                      |
| STORE_MANAGER | 必須         | users.store_id と URL の storeId が一致すること |
| STORE_STAFF   | 必須         | users.store_id と URL の storeId が一致すること |

URLに `storeId` を含まないAPIでも、対象データから店舗IDを特定できる場合は、取得したデータの store_id と users.store_id を比較する。
例として、注文ID、注文明細ID、会計ID、来店セッションIDを指定するAPIでは、対象データを取得した後に店舗所属を確認する。

## 4. JWT設計

### 4.1 JWTの発行元

JWTアクセストークンは Cognito User Pool が発行する。
Spring Boot API は JWT を発行せず、Cognito が発行したJWTを検証する OAuth2 Resource Server として動作する。

JWTの発行元は対象の Cognito User Pool とする。
Spring Boot API では issuer を検証し、想定した Cognito User Pool から発行されたトークンのみを受け入れる。

### 4.2 Spring Boot APIで検証する内容

Spring Boot API では、認証が必要なAPIに対して受け取ったJWTを検証する。
IDトークンはAPI認証には利用せず、API認証には access token を利用する。
APIでは token_use が `access` であることを確認し、スタッフ系APIで誤ってIDトークンを送信する実装ミスを防ぐ。

主な検証内容は以下とする。

| No  | 検証項目     | 内容                                                           |
| --- | ------------ | -------------------------------------------------------------- |
| 1   | 署名         | Cognito User Pool の公開鍵によりJWT署名が正しいこと            |
| 2   | issuer       | 想定した Cognito User Pool から発行されたJWTであること         |
| 3   | 有効期限     | exp が現在時刻より後であること                                 |
| 4   | 利用開始時刻 | nbf が設定されている場合、現在時刻が利用開始時刻以降であること |
| 5   | token_use    | access token として発行されたJWTであること                     |
| 6   | client_id    | access token の場合は `client_id` を検証し、想定した Cognito App Client に対して発行されたトークンであること |
| 7   | subject      | sub が存在し、users.cognito_user_id と紐づけ可能であること     |
| 8   | groups       | 必要に応じて cognito:groups から業務ロールを確認できること     |
| 9   | ユーザー状態 | users に該当ユーザーが存在し、deleted_at が NULL であること    |

JWTの署名、issuer、有効期限、token_use、client_id の検証に失敗した場合は `401 Unauthorized` を返す。
JWTは有効だが、users テーブルに該当する業務ユーザーが存在しない、論理削除されている、業務ロールや所属店舗IDの条件を満たさない場合は `403 Forbidden` を返す。

### 4.3 JWTから取得するユーザー情報

Spring Boot API は、JWTから最低限以下の情報を取得する。

| 項目              | JWT claim例             | 用途                                          |
| ----------------- | ----------------------- | --------------------------------------------- |
| CognitoユーザーID | `sub`                   | users.cognito_user_id と紐づける              |
| username          | `cognito:username` など | ログ出力、画面表示、監査補助                  |
| roles             | `cognito:groups`        | Cognito側のグループ情報確認、補助的な認可情報 |

店舗IDと業務ロールは、JWTだけに依存せず、JWTの subject から users テーブルを参照して取得する。

| 取得元 | 項目     | 用途                                             |
| ------ | -------- | ------------------------------------------------ |
| users  | id       | created_by / updated_by などの監査項目に利用する |
| users  | role     | 業務ロールによるAPI認可に利用する                |
| users  | store_id | 店舗単位アクセス制御に利用する                   |
| users  | deleted_at | 業務ユーザーが論理削除されていないことの判定に利用する |

users テーブルにパスワードは保持しない。
users.cognito_user_id と JWT の subject を紐づけて、アプリケーション側の業務ユーザーを特定する。

JWTから取得した情報と users テーブルの情報を組み合わせ、APIごとに以下を判定する。

| 判定項目     | 内容                                                   |
| ------------ | ------------------------------------------------------ |
| JWTの有効性  | Cognito が発行した有効なトークンであること             |
| 業務ユーザー | users.cognito_user_id に該当するユーザーが存在すること |
| 業務ロール   | APIに必要なロールを満たすこと                          |
| 所属店舗ID   | 店舗業務APIでは対象店舗へのアクセス権を満たすこと      |

## 5. Cognito設計

### 5.1 User Pool

User Pool は、店舗スタッフ、店長、本部管理者の認証基盤として利用する。
来店客はCognitoユーザーとして登録せず、来店客向けAPIはQRトークンと来店セッショントークンにより注文可否を制御する。

User Pool では、業務ユーザーのログインID、パスワード、パスワードポリシー、MFAなどの認証基盤側の設定を管理する。
アプリケーションDBの users テーブルにはパスワードを保持しない。

Spring Boot API は、User Pool が発行した access token を検証し、JWTの subject と users.cognito_user_id を紐づけて業務ユーザーを特定する。

### 5.2 App Client

App Client は、業務管理画面から Cognito 認証を利用するために設定する。
スタッフ系APIでは、App Client を通じて発行された access token を Authorization ヘッダーで受け取る。

API認証ではIDトークンを利用せず、access token を利用する。
Spring Boot API では token_use が `access` であることと、`client_id` が想定した App Client であることを検証する。

### 5.3 Group

Cognito Group は、認証基盤側でユーザー種別を管理し、JWTにロール相当の情報を含めるために利用する。
ただし、店舗単位の業務認可や最終的な権限制御は、アプリケーションDBの users テーブルを正として判定する。
本システムでは以下の Group を定義する。

| Group名       | 対応する業務ロール | 説明                 |
| ------------- | ------------------ | -------------------- |
| ADMIN         | ADMIN              | 本部管理者           |
| STORE_MANAGER | STORE_MANAGER      | 店長                 |
| STORE_STAFF   | STORE_STAFF        | 店舗スタッフ         |

Cognito Group はJWTの `cognito:groups` に含まれる補助情報として扱う。
最終的な業務認可はアプリケーションDBの users.role を正として判定する。
Cognito Group と users.role が一致しない場合でも、APIの認可判定では users.role を優先する。

### 5.4 User

Cognito User は、店舗スタッフ、店長、本部管理者を表す認証ユーザーとして登録する。
来店客はCognito Userとして登録しない。

アプリケーションDBの users テーブルでは、Cognito User の subject を users.cognito_user_id として保持する。
これにより、Cognito の認証ユーザーとアプリケーション上の業務ユーザーを1対1で紐づける。

users テーブルでは、業務ロール、所属店舗ID、論理削除状態など、アプリケーションの認可に必要な情報を管理する。
ユーザーが退職、異動、権限変更された場合は、Cognito User と users テーブルの両方を運用上整合させる。
ただし、API認可の最終判定は users.role、users.store_id、users.deleted_at を基に行う。

## 6. 来店客向けQR注文セキュリティ

### 6.1 固定QRトークンの扱い

固定QRトークンは、客席テーブルを識別するための推測困難なランダム値とする。
QRコードには店舗IDやテーブルIDなどの連番値を直接埋め込まず、restaurant_tables.qr_token を埋め込む。

固定QRトークンはシステム内で一意とし、QRコードから対象店舗と対象テーブルを特定するために利用する。
ただし、固定QRトークン単体では注文権限として扱わない。

来店客がQRコードを読み取った場合でも、対象テーブルが注文可能状態であり、有効な来店セッションと visitToken が存在しなければ注文を許可しない。

固定QRトークンが流出した、または不正利用が疑われる場合は、店舗管理者または本部管理者が qr_token を再発行し、QRコードを差し替える運用とする。

| 項目       | 方針                                                   |
| ---------- | ------------------------------------------------------ |
| 生成方法   | 推測困難なランダム値として生成する                     |
| 一意性     | restaurant_tables.qr_token をシステム内で一意にする    |
| 用途       | QRコードから対象店舗・対象テーブルを特定する           |
| 権限扱い   | QRトークン単体では注文権限として扱わない               |
| 露出範囲   | テーブル上の固定QRコードとして来店客に公開される       |

### 6.2 来店セッションの扱い

来店セッションは、同一テーブルにおける来店客ごとの注文・会計単位を管理する業務データである。
HTTPセッションやCognitoログインセッションではない。

visitToken は、店舗スタッフがテーブル利用開始操作を行い、対象テーブルを AVAILABLE から OCCUPIED に変更するタイミングでサーバー側が発行する。
来店客は固定QRトークンに加えて visitToken を利用し、現在の来店セッションに対する注文であることを示す。

visitToken は推測困難なランダム値として発行する。
来店客向け注文画面では必要最小限の範囲で保持し、URLクエリパラメータには含めない。
注文履歴取得やメニュー取得では HTTP ヘッダー、注文登録ではリクエストボディで送信する。

会計完了時は visit_sessions.status を CLOSED に更新し、ended_at を設定する。
これにより、過去に発行された visitToken を利用した追加注文を防止する。

visit_sessions.expires_at は、放置された来店セッションや不正利用を防ぐための最大有効期限として扱う。
通常の注文可否は table.status と visit_sessions.status を主軸に判定し、最終チェックとして expires_at が現在時刻より後であることも確認する。

| 項目       | 方針                                                               |
| ---------- | ------------------------------------------------------------------ |
| 発行契機   | テーブル利用開始時にサーバー側で発行する                           |
| 紐づき     | 店舗、テーブル、来店セッションに紐づける                           |
| 用途       | 来店単位の注文・会計を識別する                                     |
| 無効化     | 会計完了、期限切れ、来店セッション終了時に注文へ利用できなくする   |
| 保持方針   | 来店履歴としてDBに保持し、注文・会計と紐づける                     |

### 6.3 注文可能条件

来店客向け注文APIでは、Cognito認証を行わない代わりに、QRトークン、visitToken、テーブルステータス、来店セッション状態を組み合わせて注文可否を判定する。

注文を許可する主条件は、restaurant_tables.status が OCCUPIED であり、visit_sessions.status が ACTIVE であることとする。
会計待ち、清掃中、空席のテーブルでは注文を許可しない。

注文可能条件は以下とする。

| No  | 条件                                                       | 満たさない場合 |
| --- | ---------------------------------------------------------- | -------------- |
| 1   | qrToken に紐づくテーブルが存在すること                     | 注文不可       |
| 2   | visitToken に紐づく来店セッションが存在すること            | 注文不可       |
| 3   | qrToken の店舗・テーブルと、visitToken の来店セッションの店舗・テーブルが一致すること | 注文不可       |
| 4   | restaurant_tables.status が OCCUPIED であること            | 注文不可       |
| 5   | visit_sessions.status が ACTIVE であること                 | 注文不可       |
| 6   | visit_sessions.expires_at が現在時刻より後であること       | 注文不可       |
| 7   | 注文対象メニューが対象店舗に所属し、販売可能であること     | 注文不可       |

テーブルステータスごとの注文可否は以下とする。

| テーブルステータス | 注文可否 | 理由                                     |
| ------------------ | -------- | ---------------------------------------- |
| AVAILABLE          | 不可     | 空席であり、来店セッションが開始されていない |
| OCCUPIED           | 可       | 有効な来店セッションがある場合のみ注文可能 |
| PAYMENT_WAITING    | 不可     | 会計待ちであり、追加注文を受け付けない   |
| CLEANING           | 不可     | 清掃中であり、次の来店前の状態である     |

### 6.4 不正アクセス対策

来店客向けAPIでは、固定QRトークンが第三者に読み取られる可能性を前提に、QRトークン単体では注文できない設計とする。
注文登録時は、qrToken と visitToken の組み合わせが正しく、かつ対象テーブル・来店セッション・店舗が一致することをサーバー側で検証する。

不正アクセス対策は以下とする。

| No  | 対策                         | 内容                                                                 |
| --- | ---------------------------- | -------------------------------------------------------------------- |
| 1   | QRトークンの推測困難化       | 連番ではなくランダム値を利用し、URL推測によるアクセスを防止する     |
| 2   | QRトークン単体での注文禁止   | QRトークンのみでは注文登録を許可しない                               |
| 3   | visitToken検証               | 対象テーブルの有効な来店セッションに紐づく visitToken のみ許可する   |
| 4   | 状態チェック                 | OCCUPIED かつ ACTIVE の場合のみ注文を許可する                         |
| 5   | 期限チェック                 | expires_at を超過した来店セッションでは注文を許可しない               |
| 6   | 店舗・テーブル整合性確認     | qrToken、visitToken、注文メニューが同一店舗・同一来店に属することを確認する |

会計完了後、会計待ち、清掃中、空席のテーブルから送信された注文リクエストは状態不整合として拒否する。
過去の visitToken を再利用した注文や、別テーブルの qrToken と visitToken を組み合わせた注文も許可しない。

### 6.5 異常注文対策

来店客向け注文APIでは、正常な来店セッションであっても、大量注文や高額注文が発生する可能性を考慮する。
注文登録時に、注文数量、注文点数、注文金額の上限をチェックし、通常利用から外れる注文を異常注文として検知する。

異常注文対策は以下とする。

| No  | 対策               | 内容                                                             |
| --- | ------------------ | ---------------------------------------------------------------- |
| 1   | 注文数量上限       | 1明細あたりの quantity に上限を設ける                            |
| 2   | 注文明細数上限     | 1回の注文で登録できる明細数に上限を設ける                        |
| 3   | 注文金額上限       | 1回の注文合計金額が警告閾値を超える場合は異常注文として記録し、業務管理画面での確認対象とする。システム上限を超える場合のみ拒否する |
| 4   | 連続注文検知       | 短時間に同一 visitToken から大量の注文が送信された場合に検知する |
| 5   | 販売状態確認       | 販売停止中、売り切れ、論理削除済みメニューへの注文を拒否する     |

大量注文・高額注文を検知した場合は、原則として異常注文フラグまたは警告情報を付与し、業務管理画面の注文確認・厨房向け機能で確認できるようにする。
ただし、数量が0以下、明細数が許容上限を超える、システム上限金額を超える、販売不可メニューが含まれるなど、注文として成立しない場合はリクエストを拒否する。

業務上成立し得るが注意が必要な注文は、注文自体は登録し、異常注文としてログ出力および業務管理画面での確認対象とする。

## 7. API別認証・認可方針

本システムでは、API区分ごとに認証要否、必要ロール、店舗単位アクセス制御の有無を定義する。
スタッフ・店長・本部管理者向けAPIでは Cognito JWT 認証を必須とし、来店客向けAPIでは Cognito 認証を行わない。

API区分ごとの認証・認可方針は以下とする。

| API区分 | パス | 認証 | 必要ロール | 店舗単位アクセス制御 | 備考 |
| --- | --- | --- | --- | --- | --- |
| 来店客向けAPI | `/customer/**` | 不要 | なし | QRトークン・visitTokenで制御 | Cognito認証対象外 |
| 店舗スタッフ向けAPI | `/stores/{storeId}/tables/**` | 必須 | STORE_STAFF | あり | 自店舗のみ操作可能 |
| 店舗スタッフ向けAPI | `/stores/{storeId}/orders/**` | 必須 | STORE_STAFF | あり | 自店舗注文のみ参照・更新可能 |
| 店舗スタッフ向けAPI | `/stores/{storeId}/order-items/**` | 必須 | STORE_STAFF | あり | 自店舗注文明細のみ更新可能 |
| 店舗スタッフ向けAPI | `/stores/{storeId}/visit-sessions/**` | 必須 | STORE_STAFF | あり | 自店舗の来店セッションのみ |
| 店長向けAPI | `/stores/{storeId}/sales/**` | 必須 | STORE_MANAGER | あり | 自店舗売上のみ |
| 店長向けAPI | `/stores/{storeId}/payments/**` | 必須 | STORE_MANAGER | あり | 自店舗会計のみ |
| 本部管理API | `/admin/**` | 必須 | ADMIN | なし | 全店舗データ操作可能 |
| バッチ管理API | `/batches/**` | 必須 | ADMIN | なし | バッチ実行 |

メニューAPIは、参照・販売ステータス変更と、メニュー登録・更新で必要ロールを分ける。
スタッフは日次運用に必要なメニュー参照と販売ステータス変更を行えるが、メニュー情報そのものの登録・更新は店長以上のみ許可する。

| パス | メソッド | 認証 | 必要ロール | 店舗単位アクセス制御 | 備考 |
| --- | --- | --- | --- | --- | --- |
| `/stores/{storeId}/menus` | GET | 必須 | STORE_STAFF | あり | メニュー一覧取得 |
| `/stores/{storeId}/menus/{menuId}` | GET | 必須 | STORE_STAFF | あり | メニュー詳細取得 |
| `/stores/{storeId}/menus/{menuId}/status` | PATCH | 必須 | STORE_STAFF | あり | 販売ステータス変更 |
| `/stores/{storeId}/menus` | POST | 必須 | STORE_MANAGER | あり | メニュー登録 |
| `/stores/{storeId}/menus/{menuId}` | PUT | 必須 | STORE_MANAGER | あり | メニュー更新 |

必要ロール欄に `STORE_STAFF` と記載されているAPIは、ロール階層により `STORE_MANAGER` および `ADMIN` も利用可能とする。
必要ロール欄に `STORE_MANAGER` と記載されているAPIは、ロール階層により `ADMIN` も利用可能とする。

店舗単位アクセス制御が必要なAPIでは、URL の `storeId` と JWT から特定した users.store_id が一致することを確認する。
ADMIN は全店舗データにアクセスできるため、原則として店舗単位アクセス制御の制限を受けない。

URLに `storeId` を含むAPIであっても、orderItemId、orderId、paymentId、visitSessionId など子リソースIDを指定する場合は、対象データを取得したうえで store_id が URL の storeId と一致することを確認する。

## 8. パスワード管理方針

本システムでは、アプリケーションDBにユーザーパスワードを保持しない。
店舗スタッフ、店長、本部管理者の認証情報は Amazon Cognito User Pool で管理する。

アプリケーション側の users テーブルでは、Cognito User の subject、業務ロール、所属店舗ID、ユーザー名、論理削除状態など、業務認可に必要な情報のみを管理する。
フォームログインおよびBasic認証は利用しない。

| 項目 | 管理場所 | 方針 |
| --- | --- | --- |
| パスワード | Cognito | アプリケーションDBには保持しない |
| パスワードポリシー | Cognito | 最小文字数、複雑性、有効期限などはCognito設定で管理 |
| パスワードリセット | Cognito | Cognitoの機能を利用する |
| ユーザーの業務ロール | users.role | アプリケーションDBを正とする |
| 所属店舗 | users.store_id | 店舗単位アクセス制御に利用する |

## 9. セッション管理方針

本システムでは、Spring Boot API 側でログインセッションを保持しない。
スタッフ・店長・本部管理者向けAPIでは、各リクエストに付与された JWT アクセストークンを検証して認証状態を判断する。

Spring Boot API はステートレスに動作し、HTTPセッション、フォームログインセッション、サーバーサイドセッションを利用しない。
そのため、ECS Fargate 上で複数タスク構成にした場合でも、スティッキーセッションや外部セッションストアを必要としない。

一方で、visit_sessions は来店客の注文・会計単位を管理する業務データであり、HTTPセッションではない。
visit_sessions はテーブル利用開始、注文、会計、退店状態を管理するためにDBで保持する。

| セッション種別 | 管理場所 | 用途 | 方針 |
| --- | --- | --- | --- |
| スタッフ認証状態 | Cognito JWT | 業務API認証 | APIサーバーでは保持しない |
| Spring Security Session | なし | ログイン状態管理 | 使用しない |
| HTTP Session | なし | サーバー側状態管理 | 使用しない |
| 来店セッション | visit_sessions | 注文・会計単位管理 | 業務データとしてDBで管理 |

## 10. CORS方針

本システムでは、フロントエンドとAPIのオリジンが異なる場合に備えて、CORS設定を行う。
CORSでは許可するオリジン、HTTPメソッド、リクエストヘッダーを明示的に制限する。

ローカル開発環境では、フロントエンドの開発サーバーからAPIへアクセスするため、`http://localhost:3000` など必要なオリジンのみ許可する。
AWS環境では、フロントエンドの公開元が決まり次第、そのオリジンのみを許可する。

| 項目 | 方針 |
| --- | --- |
| 許可オリジン | ローカルでは開発用URLのみ。本番ではフロントエンド公開URLのみ |
| 許可メソッド | GET, POST, PUT, PATCH, OPTIONS |
| 許可ヘッダー | Authorization, Content-Type, Accept, X-Visit-Token |
| 認証情報 | Cookie認証を使わないため、原則 credentials は不要 |

本番環境では `Access-Control-Allow-Origin: *` は利用せず、許可するオリジンを明示する。

## 11. CSRF方針

本システムの業務APIでは、Cookie ベースのログインセッションを利用せず、Authorization ヘッダーの Bearer トークンにより認証を行う。
そのため、ブラウザが自動送信するCookieを前提としたCSRF対策は利用しない。

Spring Security では、REST API としてステートレスに動作させるため、CSRF保護は無効化する。

ただし、来店客向けAPIでは Cognito 認証を行わないため、CSRFではなく、QRトークン、visitToken、テーブルステータス、来店セッション状態による注文可否チェックを必ず行う。

| API区分 | CSRF方針 | 理由 |
| --- | --- | --- |
| 店舗業務API | 無効化 | Cookie認証ではなくAuthorizationヘッダーのJWTを利用するため |
| 本部管理API | 無効化 | Cookie認証ではなくAuthorizationヘッダーのJWTを利用するため |
| バッチ管理API | 無効化 | Cookie認証ではなくAuthorizationヘッダーのJWTを利用するため |
| 来店客向けAPI | 無効化 | Cookie認証を利用せず、QRトークン・visitTokenで制御するため |

## 12. ログ出力方針

本システムでは、セキュリティ上重要な操作や認証・認可エラーをログ出力し、不正アクセス調査、障害調査、運用確認に利用する。

ログには、操作ユーザー、対象リソース、処理結果、エラー内容を記録する。
ただし、パスワード、JWTアクセストークン、visitToken、qrToken などの機密情報はログに出力しない。
調査上必要な場合でも、これらの値そのものは出力せず、ハッシュ化した値、または末尾数文字のみを出力する。

### 12.1 ログ出力対象

| No | 出力対象 | 内容 |
| --- | --- | --- |
| 1 | 認証失敗 | JWT未指定、JWT不正、期限切れなど |
| 2 | 認可失敗 | 必要ロール不足、自店舗外データへのアクセス |
| 3 | 来店客向け注文拒否 | 無効なQRトークン、無効なvisitToken、注文不可ステータス |
| 4 | スタッフ操作 | テーブル利用開始、ステータス変更、調理ステータス更新、会計処理 |
| 5 | 管理操作 | 店舗登録・更新・論理削除、メニュー登録・更新 |
| 6 | バッチ操作 | バッチ実行開始、正常終了、異常終了 |
| 7 | 異常注文検知 | 大量注文、高額注文、短時間の連続注文 |

### 12.2 ログに含める情報

| 項目 | 内容 |
| --- | --- |
| timestamp | ログ出力日時 |
| level | INFO、WARN、ERROR |
| userId | 業務ユーザーID。来店客向けAPIでは出力しない |
| role | 業務ロール |
| storeId | 対象店舗ID |
| requestPath | リクエストパス |
| httpMethod | HTTPメソッド |
| action | 実行した業務操作 |
| result | SUCCESS、FAILURE |
| errorCode | エラー発生時のエラーコード |
| message | ログメッセージ |

来店客向けAPIでは userId は出力せず、必要に応じて storeId、tableId、visitSessionId、orderId などの内部IDを出力する。
ただし、visitToken や qrToken の値そのものは出力しない。

### 12.3 ログレベル方針

| ログレベル | 用途 |
| --- | --- |
| INFO | 正常な業務操作、バッチ開始・終了 |
| WARN | 認証失敗、認可失敗、注文不可、異常注文検知 |
| ERROR | 想定外エラー、DBエラー、バッチ異常終了 |

### 12.4 機密情報のログ出力禁止

以下の情報はログに出力しない。

| No | 出力禁止情報 | 理由 |
| --- | --- | --- |
| 1 | パスワード | Cognitoで管理される認証情報のため |
| 2 | JWTアクセストークン | 漏えい時にAPI不正利用につながるため |
| 3 | visitToken | 来店セッションの不正利用につながるため |
| 4 | qrToken | テーブル識別情報の不正利用につながるため |
| 5 | 個人情報 | 不要な個人情報保持を避けるため |

qrToken、visitToken、JWTアクセストークンはログに値そのものを出力しない。
調査上必要な場合は、ハッシュ化した値、または末尾数文字のみを出力する。

### 12.5 CloudWatch連携方針

AWS環境では、ECS Fargate 上の Spring Boot API の標準出力ログを CloudWatch Logs に送信する。
アプリケーションログ、認証・認可エラーログ、バッチ実行ログを CloudWatch Logs で確認できるようにする。

ログは運用確認と障害調査を目的として利用し、必要に応じてロググループ、ログストリーム、保持期間を設定する。
ログ保持期間はコストと調査要件を考慮して設定し、不要な長期保存は行わない。

## 13. セキュリティテスト観点

本システムでは、認証、認可、店舗単位アクセス制御、来店客向けQR注文制御を重点的にテストする。
特に、JWTが有効であってもロール不足や自店舗外アクセスを拒否できること、QRトークン単体では注文できないことを確認する。

### 13.1 認証テスト

| No | テスト観点 | 期待結果 |
| --- | --- | --- |
| 1 | Authorization ヘッダーなしで店舗業務APIへアクセスする | 401 Unauthorized |
| 2 | Bearer形式でない Authorization ヘッダーでアクセスする | 401 Unauthorized |
| 3 | 期限切れJWTでアクセスする | 401 Unauthorized |
| 4 | 不正な署名のJWTでアクセスする | 401 Unauthorized |
| 5 | token_use が access ではないJWTでアクセスする | 401 Unauthorized |
| 6 | 有効なJWTで店舗業務APIへアクセスする | 認証成功 |
| 7 | 有効なJWTだが users に該当ユーザーが存在しない状態でアクセスする | 403 Forbidden |
| 8 | 有効なJWTだが users.deleted_at が NULL ではないユーザーでアクセスする | 403 Forbidden |
| 9 | 想定外の Cognito App Client 向けに発行されたJWTでアクセスする | 401 Unauthorized |

### 13.2 ロール認可テスト

| No | テスト観点 | 期待結果 |
| --- | --- | --- |
| 1 | STORE_STAFF が STORE_MANAGER 権限APIへアクセスする | 403 Forbidden |
| 2 | STORE_MANAGER が STORE_STAFF 権限APIへアクセスする | アクセス成功 |
| 3 | ADMIN が STORE_MANAGER 権限APIへアクセスする | アクセス成功 |
| 4 | ADMIN が本部管理APIへアクセスする | アクセス成功 |
| 5 | STORE_STAFF が本部管理APIへアクセスする | 403 Forbidden |

### 13.3 店舗単位アクセス制御テスト

| No | テスト観点 | 期待結果 |
| --- | --- | --- |
| 1 | STORE_STAFF が自店舗のテーブル一覧を取得する | 200 OK |
| 2 | STORE_STAFF が他店舗のテーブル一覧を取得する | 403 Forbidden |
| 3 | STORE_MANAGER が自店舗の売上を取得する | 200 OK |
| 4 | STORE_MANAGER が他店舗の売上を取得する | 403 Forbidden |
| 5 | orderId、orderItemId、paymentId、visitSessionId 指定時に他店舗データへアクセスする | 403 Forbidden |

### 13.4 来店客向けQR注文制御テスト

| No | テスト観点 | 期待結果 |
| --- | --- | --- |
| 1 | 有効なQRトークンでQR注文画面を表示する | 200 OK |
| 2 | テーブルが AVAILABLE の状態でQR注文画面を表示する | 200 OK、orderAvailable=false |
| 3 | 存在しないQRトークンでQR注文画面を表示する | 404 Not Found |
| 4 | テーブルが AVAILABLE の状態で注文しようとする | 409 Conflict |
| 5 | テーブルが PAYMENT_WAITING の状態で注文しようとする | 409 Conflict |
| 6 | テーブルが CLEANING の状態で注文しようとする | 409 Conflict |
| 7 | visitToken なしでメニュー一覧を取得する | 400 Bad Request |
| 8 | 無効な visitToken で注文登録する | 403 Forbidden |
| 9 | 期限切れ visitToken で注文登録する | 409 Conflict |
| 10 | 別テーブルの qrToken と visitToken を組み合わせて注文する | 403 Forbidden |
| 11 | 販売停止中メニューを注文する | 409 Conflict |
| 12 | qrToken のみで注文登録する | 400 Bad Request |
| 13 | 有効な qrToken と別店舗の visitToken を組み合わせて注文する | 403 Forbidden |

### 13.5 異常注文テスト

| No | テスト観点 | 期待結果 |
| --- | --- | --- |
| 1 | quantity が0の注文を登録する | 400 Bad Request |
| 2 | quantity が負数の注文を登録する | 400 Bad Request |
| 3 | 1回の注文で明細数上限を超える | 400 Bad Request |
| 4 | 1回の注文金額が警告閾値を超える | 注文登録成功、異常注文としてログ出力 |
| 5 | 短時間に同一 visitToken から連続注文する | 異常注文として検知 |

### 13.6 CSRF・CORSテスト

| No | テスト観点 | 期待結果 |
| --- | --- | --- |
| 1 | 許可されたオリジンからAPIへアクセスする | アクセス可能 |
| 2 | 許可されていないオリジンからAPIへアクセスする | ブラウザによりCORSエラーとしてブロックされる |
| 3 | Authorization ヘッダーを含むリクエストが許可される | アクセス可能 |
| 4 | X-Visit-Token ヘッダーを含むリクエストが許可される | アクセス可能 |
| 5 | Cookieなし、CSRFトークンなしでJWT認証APIを実行する | CSRFでは拒否されず、JWT認証で判定される |

### 13.7 ログ出力テスト

| No | テスト観点 | 期待結果 |
| --- | --- | --- |
| 1 | 認証失敗時にWARNログが出力される | JWT値そのものは出力されない |
| 2 | 認可失敗時にWARNログが出力される | ユーザーID、対象storeId、エラーコードが出力される |
| 3 | 会計処理成功時にINFOログが出力される | 操作ユーザー、店舗ID、来店セッションIDが出力される |
| 4 | 異常注文検知時にWARNログが出力される | visitTokenの値そのものは出力されない |
| 5 | QR注文拒否時にWARNログが出力される | qrToken、visitToken の値そのものは出力されない |
| 6 | 認証失敗ログを確認する | JWTアクセストークンの値そのものは出力されない |
